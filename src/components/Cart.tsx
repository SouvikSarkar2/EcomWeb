"use client";

import { ShoppingCart } from "lucide-react";

const Cart = () => {
  return (
    <div className="flex gap-2 cursor-pointer">
      Cart
      <ShoppingCart />
    </div>
  );
};

export default Cart;
